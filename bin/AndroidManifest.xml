<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hbcmcc.sclumin"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission   android:name="android.permission.DISABLE_KEYGUARD"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <service android:name="com.hbcmcc.sclumin.service.ScLuminService"></service>
         <activity
            android:name="com.hbcmcc.sclumin.service.BootstrapActivity"
            android:label="@string/app_name" >
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hbcmcc.sclumin.loopview.AdsActivity"
            android:label="@string/app_name" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name="com.hbcmcc.sclumin.lock.LockActivity"
            android:label="@string/title_lock" 
            android:windowSoftInputMode="adjustResize|stateVisible"
            android:theme="@style/LockTheme"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name="com.hbcmcc.sclumin.lock.UnlockActivity"
            android:label="@string/title_unlock" 
            android:windowSoftInputMode="adjustResize|stateVisible"
            android:theme="@style/LockTheme"
            android:launchMode="singleTask">
         </activity>
    </application>

</manifest>
